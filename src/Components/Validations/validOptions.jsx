// export const isValidOption = async (selectedOption) => {
//     try {
//         if (!selectedOption || selectedOption.value === undefined) {
//             return { valid: false, error: '*Debes seleccionar uno' };
//         } else {
//             return { valid: true };
//         }
//     } catch (error) {
//         return { valid: false, error: '* Error al validar el tipo' };
//     }
// };